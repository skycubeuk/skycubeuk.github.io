services:
  oauth-proxy:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "3000:3000"
    volumes:
      - proxy-logs:/app/logs
    networks:
      - proxy

volumes:
  proxy-logs:

networks:
  proxy:
    external: true

