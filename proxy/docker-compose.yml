services:
  oauth-proxy:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - proxy-logs:/app/logs
    networks:
      - proxy

volumes:
  proxy-logs:

networks:
  proxy:
    external: true

