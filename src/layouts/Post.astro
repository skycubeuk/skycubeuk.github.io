---
import Base from './Base.astro';
const { frontmatter } = Astro.props;
const title = frontmatter?.title;
const date: Date | undefined = frontmatter?.date ? new Date(frontmatter.date) : undefined;
---

<Base title={title}>
  <article class="max-w-3xl mx-auto px-4 sm:px-6 py-12">
    {title && (
      <header class="mb-8">
        <h1 class="text-3xl font-bold text-gray-900">{title}</h1>
        {date && (
          <time datetime={date.toISOString()} class="text-sm text-gray-500 mt-2 block">
            {date.toLocaleDateString('en-GB', { day: 'numeric', month: 'long', year: 'numeric' })}
          </time>
        )}
      </header>
    )}
    <div class="prose prose-lg max-w-none prose-a:text-[#552d62] prose-headings:text-gray-900">
      <slot />
    </div>
    <div class="mt-10 pt-6 border-t border-gray-200">
      <a href="/posts" class="text-sm text-[#552d62] hover:underline">← All news posts</a>
    </div>
  </article>
</Base>
